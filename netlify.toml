# Netlify 설정 - Supabase 도시락 주문 시스템
[build]
  base = "supabase"
  publish = "supabase"
  command = "echo 'Static site - no build needed'"

# SPA 라우팅 지원 (모든 경로를 index.html로 리다이렉트)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


